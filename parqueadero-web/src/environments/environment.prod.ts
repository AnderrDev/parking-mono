export const environment = {
  production: true,
  supabaseUrl: '',       // se inyecta en CI con variables de entorno
  supabaseAnonKey: '',   // se inyecta en CI con variables de entorno
  powerSyncUrl: '',      // se inyecta en CI cuando PowerSync esté configurado
};
