# Copia a .env.local (no commitear). Variables consumidas vía environment.ts en build.

# Supabase project (anon key, expuesta al cliente — RLS protege)
SUPABASE_URL=
SUPABASE_ANON_KEY=

# PowerSync endpoint (sync server URL)
POWERSYNC_URL=

# Microservicio DIAN (en Fase 9 puede quedar vacío -> stub local en Edge Function)
DIAN_FE_SERVICE_URL=
